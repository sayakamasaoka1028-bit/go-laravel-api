<!-- resources/views/contact/index.blade.php -->
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FashionablyLate</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center p-6">
    <div class="w-full max-w-lg">
     <h1 class="text-3xl font-bold mb-6 text-center border-b-2 border-gray-300 pb-3">
        FashionablyLate</h1>

<p class="text-center text-gray-600 mb-8" text-3xl>Contact</p>
        <form action="{{ route('contact.confirm') }}" method="POST" class="bg-white p-6 rounded shadow-md space-y-4">
            @csrf

            <!-- お名前 -->
        <div>
            <label class="block font-medium mb-1">お名前</label>
            <input type="text" name="name" class="border p-2 w-full rounded" value="{{ old('name') }}">
            @error('name') <p class="text-red-500 text-sm mt-1">{{ $message }}</p> @enderror
        </div>

            <!-- 性別 -->
            <div>
                <label class="block font-medium mb-1">性別</label>
                <select name="gender" class="border p-2 w-full rounded">
                    <option value="">選択してください</option>
                    <option value="1" @selected(old('gender')=='1')>男性</option>
                    <option value="2" @selected(old('gender')=='2')>女性</option>
                    <option value="3" @selected(old('gender')=='3')>その他</option>
                </select>
                @error('gender') <p class="text-red-500 text-sm mt-1">{{ $message }}</p> @enderror
            </div>

            <!-- メール -->
            <div>
                <label class="block font-medium mb-1">メール</label>
                <input type="email" name="email" class="border p-2 w-full rounded" value="{{ old('email') }}">
                @error('email') <p class="text-red-500 text-sm mt-1">{{ $message }}</p> @enderror
            </div>

            <!-- 電話番号 -->
            <div class="mb-4">
                <label class="block font-medium mb-1">電話番号</label>
                <input type="text" name="tel" class="border p-2 w-full rounded" placeholder="例: 09012345678" value="{{ old('tel') }}">
                @error('tel')
                <p class="text-red-500 text-sm mt-1">{{ $message }}</p>
                @enderror
            </div>


            <!-- 住所 -->
            <div>
                <label class="block font-medium mb-1">住所</label>
                <input type="text" name="address" class="border p-2 w-full rounded" value="{{ old('address') }}">
                @error('address') <p class="text-red-500 text-sm mt-1">{{ $message }}</p> @enderror
            </div>

            <!-- 建物名 -->
            <div>
                <label class="block font-medium mb-1">建物名</label>
                <input type="text" name="building_name" class="border p-2 w-full rounded" value="{{ old('building_name') }}">
            </div>

            <!-- お問い合わせ種類 -->
            <div>
                <label class="block font-medium mb-1">お問い合わせ種類</label>
                <select name="category_id" class="border p-2 w-full rounded">
                    <option value="">選択してください</option>
                    @foreach($categories as $category)
                        <option value="{{ $category->id }}" @selected(old('category_id')==$category->id)>
                        {{ $category->content }}
                        </option>
                    @endforeach
                </select>
                @error('category_id') <p class="text-red-500 text-sm mt-1">{{ $message }}</p> @enderror
            </div>

            <!-- お問い合わせ内容 -->
            <div>
                <label class="block font-medium mb-1">お問い合わせ内容</label>
                <textarea name="detail" class="border p-2 w-full rounded h-32">{{ old('detail') }}</textarea>
                @error('detail') <p class="text-red-500 text-sm mt-1">{{ $message }}</p> @enderror
            </div>

            <!-- 確認ボタン -->
            <div class="text-center">
            <button
            type="submit"
            class="bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded">
            確認画面
            </button>
            </div>
        </form>
    </div>
</body>
</html>
